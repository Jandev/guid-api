name: Azure Static Web Apps CI/CD

# IMPORTANT: This workflow requires a DEPLOYMENT_PAT secret with admin permissions
# See .github/SETUP.md for complete setup instructions

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

env:
  AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP || 'rg-guid-api' }}
  AZURE_STATIC_WEB_APP_NAME: ${{ vars.AZURE_STATIC_WEB_APP_NAME || 'guid-api-swa' }}
  AZURE_LOCATION: ${{ vars.AZURE_LOCATION || 'westeurope' }}
  CUSTOM_DOMAIN_NAME: ${{ vars.CUSTOM_DOMAIN_NAME || 'guid.codes' }}
  ENABLE_CUSTOM_DOMAIN: ${{ vars.ENABLE_CUSTOM_DOMAIN || 'true' }}

jobs:
  infrastructure:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Deploy Infrastructure
    outputs:
      deployment-token: ${{ steps.get-swa-token.outputs.deployment-token }}
      static-web-app-url: ${{ steps.get-swa-token.outputs.static-web-app-url }}
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_DEV }}

      - name: Validate Azure Connection
        run: |
          echo "Testing Azure CLI connection..."
          az account show
          echo "Subscription: ${{ secrets.AZURE_SUBSCRIPTION_ID }}"

      - name: Deploy Bicep Infrastructure
        id: deploy-infrastructure
        uses: azure/arm-deploy@v2
        with:
          subscriptionId: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          scope: subscription
          region: ${{ env.AZURE_LOCATION }}
          template: ./deployment/staticwebapp.bicep
          parameters: >
            resourceGroupName=${{ env.AZURE_RESOURCE_GROUP }}
            staticWebAppName=${{ env.AZURE_STATIC_WEB_APP_NAME }}
            location=${{ env.AZURE_LOCATION }}
            repositoryUrl=${{ github.server_url }}/${{ github.repository }}
            repositoryToken=${{ secrets.DEPLOYMENT_PAT }}
            branch=${{ github.event_name == 'pull_request' && github.head_ref || github.ref_name }}
            sku=Standard
            customDomainName=${{ env.CUSTOM_DOMAIN_NAME }}
            enableCustomDomain=${{ env.ENABLE_CUSTOM_DOMAIN }}

      - name: Get Static Web App Deployment Token
        id: get-swa-token
        run: |
          echo "Waiting for Static Web App to be fully provisioned..."
          sleep 30
          
          # Get the deployment token from the Static Web App
          echo "Retrieving Static Web App deployment token..."
          SWA_TOKEN=$(az staticwebapp secrets list \
            --name ${{ env.AZURE_STATIC_WEB_APP_NAME }} \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --query "properties.apiKey" \
            --output tsv)
          
          if [ -z "$SWA_TOKEN" ]; then
            echo "Error: Failed to retrieve Static Web App token"
            exit 1
          fi
          
          echo "deployment-token=$SWA_TOKEN" >> $GITHUB_OUTPUT
          
          # Get the default hostname
          echo "Retrieving Static Web App hostname..."
          SWA_URL=$(az staticwebapp show \
            --name ${{ env.AZURE_STATIC_WEB_APP_NAME }} \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --query "defaultHostname" \
            --output tsv)
          
          if [ -z "$SWA_URL" ]; then
            echo "Error: Failed to retrieve Static Web App hostname"
            exit 1
          fi
          
          echo "static-web-app-url=https://$SWA_URL" >> $GITHUB_OUTPUT
          echo "✅ Successfully retrieved Static Web App details"

  build_and_deploy:
    needs: infrastructure
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Application
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'package.json'

      - name: Install dependencies
        run: npm install

      - name: Build application
        run: npm run build

      - name: Deploy to Azure Static Web Apps
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ needs.infrastructure.outputs.deployment-token }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          ###### Repository/Build Configurations ######
          app_location: "/src"
          api_location: "/api"
          output_location: "/dist"
          ###### End of Repository/Build Configurations ######

      - name: Deployment Summary
        run: |
          echo "🎉 Deployment completed successfully!"
          echo "🌐 Application URL: ${{ needs.infrastructure.outputs.static-web-app-url }}"
          echo "🌍 Custom Domain: https://${{ env.CUSTOM_DOMAIN_NAME }}"
          echo "📋 Static Web App: ${{ env.AZURE_STATIC_WEB_APP_NAME }}"
          echo "📁 Resource Group: ${{ env.AZURE_RESOURCE_GROUP }}"
          echo ""
          echo "🔧 DNS Configuration Required:"
          echo "Create a CNAME record: ${{ env.CUSTOM_DOMAIN_NAME }} -> [Check Azure portal for target]"
          echo "💡 Note: SSL certificate will be automatically provisioned after DNS validation"

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_DEV }}

      - name: Get Static Web App Deployment Token
        id: get-swa-token
        run: |
          echo "Retrieving Static Web App deployment token for cleanup..."
          SWA_TOKEN=$(az staticwebapp secrets list \
            --name ${{ env.AZURE_STATIC_WEB_APP_NAME }} \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --query "properties.apiKey" \
            --output tsv 2>/dev/null) || echo ""
          
          if [ -z "$SWA_TOKEN" ]; then
            echo "Warning: Could not retrieve Static Web App token. Resource may not exist."
            echo "deployment-token=SKIP" >> $GITHUB_OUTPUT
          else
            echo "deployment-token=$SWA_TOKEN" >> $GITHUB_OUTPUT
          fi

      - name: Close Pull Request
        id: closepullrequest
        if: steps.get-swa-token.outputs.deployment-token != 'SKIP'
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ steps.get-swa-token.outputs.deployment-token }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "close"