# Local environment variables (not committed to version control)
# Copy this file to .env.local and modify as needed for your local development

# API URL for local development
# Uncomment and modify the line below if you're running the API locally
# GUID_API_URL=http://localhost:7071

# Or use a different staging/test environment
# GUID_API_URL=https://api-staging.guid.codes