# Development environment variables
# This file can be used to override default values during development

# API URL for development (you can change this to localhost if running API locally)
GUID_API_URL=https://api.guid.codes